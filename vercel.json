{
  "framework": "nextjs",
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm install --include=dev --no-audit --no-fund && npx prisma db push --skip-generate && npm run build"
}
